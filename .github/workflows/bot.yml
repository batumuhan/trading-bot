name: Trading Bot

on:
  schedule:
    - cron: '30 7 * * 1-5'
    - cron: '30 10 * * 1-5'
    - cron: '0 14 * * 1-5'
    - cron: '0 */2 * * *'
  workflow_dispatch:
    inputs:
      gorev:
        description: 'Gorev'
        required: true
        default: 'KRIPTO'
        type: choice
        options:
          - SABAH
          - OGLE
          - AKSAM
          - KRIPTO

jobs:
  calistir:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Kutuphaneleri kur
        run: |
          pip install yfinance pandas numpy python-telegram-bot requests beautifulsoup4

      - name: Token yaz
        env:
          TG_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TG_CHAT: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          python3 -c "
          import os
          with open('config.py', 'r', encoding='utf-8') as f:
              c = f.read()
          c = c.replace('8731413161:AAG8TR91BrQ_OTh-tRBcOSx4fkzk0ke6TUk', os.environ['TG_TOKEN'])
          c = c.replace('\"947636416\"', '\"' + os.environ['TG_CHAT'] + '\"')
          with open('config.py', 'w', encoding='utf-8') as f:
              f.write(c)
          print('Token yazildi')
          "

      - name: Gorevi belirle
        id: gorev
        env:
          INPUT_GOREV: ${{ github.event.inputs.gorev || '' }}
        run: |
          python3 -c "
          import os
          from datetime import datetime, timezone, timedelta
          manuel = os.environ.get('INPUT_GOREV', '')
          if manuel:
              gorev = manuel
          else:
              tr = (datetime.now(timezone.utc) + timedelta(hours=3)).hour
              gun = datetime.now(timezone.utc).weekday()
              if gun < 5 and tr == 10:
                  gorev = 'SABAH'
              elif gun < 5 and tr == 13:
                  gorev = 'OGLE'
              elif gun < 5 and tr == 17:
                  gorev = 'AKSAM'
              else:
                  gorev = 'KRIPTO'
          print('Gorev: ' + gorev)
          with open(os.environ['GITHUB_OUTPUT'], 'a') as f:
              f.write('gorev=' + gorev + '\n')
          "

      - name: Calistir
        run: python gunluk_tara.py ${{ steps.gorev.outputs.gorev }}

      - name: sim.json kaydet
        run: |
          git config user.name "bot"
          git config user.email "bot@bot.com"
          git add sim.json || true
          git diff --staged --quiet || git commit -m "sim guncellendi"
          git push || true
